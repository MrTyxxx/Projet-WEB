{% extends "monespace.html.twig" %}
{% block contenu_espace %}

<div class="container-fluid px-2">

  <!-- Filtres -->
  <div class="d-flex gap-2 mb-3 flex-wrap">
    <button class="btn btn-dark btn-sm">Tous les Pilotes</button>
    <input type="text" class="form-control form-control-sm w-auto" placeholder="Recherche par Noms" id="searchNom">
    <select class="form-select form-select-sm w-auto" id="filterCampus">
      <option value="">Campus</option>
      <option value="Reims">Reims</option>
      <option value="Strasbourg">Strasbourg</option>
    </select>
  </div>

  <!-- Tableau -->
  <div class="border rounded-3 p-3">
    <table class="table table-bordered table-sm text-center align-middle" id="tablePilotes">
      <thead class="table-light">
        <tr>
          <th>N°</th>
          <th>Pilotes</th>
          <th>Email</th>
          <th>Promo(s) gérée(s)</th>
          <th>Contact</th>
          <th>Campus</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>1</td>
          <td>Dupont Marie</td>
          <td>marie.dupont@yourjob.com</td>
          <td><span class="badge bg-secondary me-1">A1</span><span class="badge bg-secondary me-1">A2</span><span class="badge bg-secondary">A3</span></td>
          <td>06 10 20 30 40</td>
          <td>Reims</td>
          <td>
            <a href="#" class="btn btn-sm btn-outline-secondary me-1">✏️</a>
            <a href="#" class="btn btn-sm btn-outline-danger">✖</a>
          </td>
        </tr>
        <tr>
          <td>2</td>
          <td>Renaud Pierre</td>
          <td>pierre.renaud@yourjob.com</td>
          <td><span class="badge bg-secondary me-1">A4</span><span class="badge bg-secondary">A5</span></td>
          <td>06 50 60 70 80</td>
          <td>Strasbourg</td>
          <td>
            <a href="#" class="btn btn-sm btn-outline-secondary me-1">✏️</a>
            <a href="#" class="btn btn-sm btn-outline-danger">✖</a>
          </td>
        </tr>
        <tr>
          <td>3</td>
          <td>Leclerc Sophie</td>
          <td>sophie.leclerc@yourjob.com</td>
          <td><span class="badge bg-secondary">A2</span></td>
          <td>06 90 80 70 60</td>
          <td>Reims</td>
          <td>
            <a href="#" class="btn btn-sm btn-outline-secondary me-1">✏️</a>
            <a href="#" class="btn btn-sm btn-outline-danger">✖</a>
          </td>
        </tr>
      </tbody>
    </table>

    <div class="d-flex justify-content-between align-items-center mt-2">
      <button class="btn btn-outline-secondary btn-sm">Précédent</button>
      <a href="/espace/pilotes/creer" class="btn btn-dark btn-sm">➕</a>
      <button class="btn btn-outline-secondary btn-sm">Suivant</button>
    </div>
  </div>

</div>

<script>
  document.getElementById('searchNom').addEventListener('input', filtrer);
  document.getElementById('filterCampus').addEventListener('change', filtrer);

  function filtrer() {
    const nom = document.getElementById('searchNom').value.toLowerCase();
    const campus = document.getElementById('filterCampus').value;
    document.querySelectorAll('#tablePilotes tbody tr').forEach(tr => {
      const nomCell = tr.cells[1].textContent.toLowerCase();
      const campusCell = tr.cells[5].textContent.trim();
      const matchNom = nomCell.includes(nom);
      const matchCampus = campus === '' || campusCell === campus;
      tr.style.display = matchNom && matchCampus ? '' : 'none';
    });
  }
</script>

{% endblock %}